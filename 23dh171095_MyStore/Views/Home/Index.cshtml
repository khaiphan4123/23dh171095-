@model _23dh171095_MyStore.Models.ViewsModel.HomeProductVM

@{
    ViewBag.Title = "Customer Home Page";
}

<!-- Link CSS -->
<link rel="stylesheet" type="text/css" href="~/Content/MyStyle/CustomerHome.css" />

<!-- Banner Section -->
<section class="container-fluid banner">
    <div class="banner-container">
        <h2>Chào mừng đến trang web E-commerce</h2>
        <p>Mua sắm trực tuyến với hàng ngàn sản phẩm đa dạng và chất lượng</p>
        <a href="@Url.Action("Index", "Products")" class="btnBanner">Bắt đầu mua sắm</a>
    </div>
</section>

<!-- Search Section -->
<section class="container-fluid searchProd">
    <h2>Tìm kiếm sản phẩm</h2>
    @using (Html.BeginForm("Index", "Home", FormMethod.Get))
    {
        <div class="form-group search-container">
            @Html.TextBoxFor(m => m.SearchTerm, new
            {
                @class = "search-input",
                placeholder = "Nhập tên, mô tả hoặc chủng loại sản phẩm"
            })
            <button type="submit" class="search-button">Tìm kiếm</button>
        </div>
    }
</section>

<hr />

<!-- Featured Products -->
@if (Model.NewProducts != null && Model.NewProducts.Any())
{
    @Html.Partial("_PVFeatureProduct", Model)
}



<!-- New Products with Pagination -->
<div>
    @if (Model.NewProducts != null && Model.NewProducts.Any())
    {
        @Html.Partial("_PVNewProduct", Model)
    }

</div>

<!-- Pagination -->
<div class="pagination-container text-center">
    @Html.IPagedListPager(Model.NewProducts, page => Url.Action("Index", new { page, searchTerm = Model.SearchTerm }))
</div>
    </section>
}
else
{
<p class="no-results">Không tìm thấy sản phẩm phù hợp với từ khóa.</p>
}
